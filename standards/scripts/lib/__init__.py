"""Shared helpers for the CRV Agent Skills repository validators.

These modules are imported by the scripts in ``standards/scripts/``. They are
repo tooling, not skill-bundled scripts, so they may use third-party
dependencies (see the two-tier Python policy in docs/design-principles.md).
"""

from __future__ import annotations
