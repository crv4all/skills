name: ci
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: mvn -B verify
        env:
          DATABASE_PASSWORD: ${{ secrets.DATABASE_PASSWORD }}
