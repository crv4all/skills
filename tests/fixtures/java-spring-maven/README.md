# Order service

A Spring Boot service for order intake.

## Modules

- `core/` — domain model and business rules
- `api/` — REST controllers
- `worker/` — asynchronous processing

Build with `mvn verify`.
