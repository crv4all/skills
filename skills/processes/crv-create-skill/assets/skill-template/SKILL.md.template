---
name: {{NAME}}
description: >-
  {{DESCRIPTION}}
license: Apache-2.0
metadata:
  owner: {{OWNER}}
  layer: {{LAYER}}
  maturity: draft
  execution: subagent
  model-tier: economy
---

# {{TITLE}}

<!--
Write to an agent that is already competent. This file exists to correct what a
capable agent gets WRONG, not to teach it the basics.

Budgets: 500 lines / 25,000 characters / 5,000 tokens. Detail goes in
references/, which loads on demand. Delete every comment like this one before
committing.
-->

<One or two sentences: what this is for, and the single thing it gets right
that an agent otherwise gets wrong.>

## Execution

<!--
MANDATORY. Every CRV skill declares this, and validate_frontmatter.py fails the
build without it. Keep the shape; change only the sentence explaining why a
subagent is right for this skill.
-->

**Delegate to a subagent. Do not run this in the main session.** <One sentence:
what context this skill accumulates that the user does not want left in their
conversation.>

**Model tier: `economy`** — the cheapest model that can follow instructions and
call tools. Before spawning the subagent, ask once:

> Running `{{NAME}}` in a subagent on the **economy** tier. Reply `balanced` or
> `frontier` to run it on a stronger model, or continue to accept the default.

Ask once per invocation, before any work starts. Skip the question only when
the user has already stated a tier preference in this session or in the
project's agent configuration.

**Never silently escalate.** If the subagent is out of its depth, stop and say
so rather than re-running on a bigger model.

If the harness has no subagent mechanism, say so plainly and run inline.

## What this produces

<The output contract, stated so a reader could check it. Not "produce
documentation" but "these files exist, each has these sections, every claim
carries evidence".>

## When not to use this

- <Near neighbour, and the skill or action that should handle it instead>
- <A case that looks similar but is out of scope>

## Procedure

1. **<Step>.** <What to do. Say why the order matters if it does — an agent
   under pressure reorders steps whose purpose it cannot see.>
2. **<Step>.** <...>
3. **<Step>.** <...>

<Pair every rule with its reason. A rule with no rationale is discarded the
moment the surrounding code disagrees with it.>

## When a step fails

<What to do. Unhandled failure is where an agent starts improvising, and
improvisation is what this skill exists to prevent.>

## Validation

Before reporting done:

- [ ] <A checkable assertion>
- [ ] <A checkable assertion>

<If any check fails, fix and re-check. Never report completion with a known
failure, and never report a check as passed that you did not run.>

## References

- [references/<topic>.md](references/<topic>.md) — <what is in it>
